{%- comment -%}
  Renders an ingredient card with icon, title, and description.

  Usage:
    {% render 'kw-ingredient-card', icon: block.settings.icon, title: block.settings.title, description: block.settings.description %}

  Params:
    - icon: image object
    - title: string
    - description: string
{%- endcomment -%}

<div class="kw-ingredient-card">
  {% if icon != blank %}
    <img
      src="{{ icon | image_url: width: 80 }}"
      alt=""
      class="kw-ingredient-card__icon"
      loading="lazy"
    >
  {% endif %}
  <h4 class="kw-ingredient-card__title">{{ title }}</h4>
  <p class="kw-ingredient-card__desc">{{ description }}</p>
</div>
